# System Architecture

## 📐 Tổng quan kiến trúc

Documentation về kiến trúc hệ thống UrbanReflex API.

---

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Web App    │  │  Mobile App  │  │   IoT Device │          │
│  │  (Browser)   │  │   (Native)   │  │   (Sensor)   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┼──────────────────┘                   │
│                            │                                      │
│                   X-API-Key Header                               │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API GATEWAY LAYER                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Nginx / Load Balancer                   │  │
│  │  - SSL/TLS Termination                                     │  │
│  │  - Rate Limiting                                           │  │
│  │  - Request Routing                                         │  │
│  └─────────────────────────┬─────────────────────────────────┘  │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                  Next.js Application                       │  │
│  │                                                            │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │  API Routes  │  │  Middleware  │  │   Pages      │   │  │
│  │  │              │  │              │  │              │   │  │
│  │  │ /api/v1/*    │  │ - Auth       │  │ /api-keys    │   │  │
│  │  │              │  │ - CORS       │  │ /api-docs    │   │  │
│  │  │              │  │ - Logging    │  │              │   │  │
│  │  └──────┬───────┘  └──────┬───────┘  └──────────────┘   │  │
│  │         │                  │                              │  │
│  └─────────┼──────────────────┼──────────────────────────────┘  │
│            │                  │                                  │
│            ▼                  ▼                                  │
│  ┌─────────────────┐  ┌─────────────────┐                      │
│  │  Auth Service   │  │  Validation     │                      │
│  │  - API Key      │  │  - Input        │                      │
│  │  - Permissions  │  │  - Schema       │                      │
│  └─────────────────┘  └─────────────────┘                      │
│                                                                   │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │   PostgreSQL     │  │      Redis       │  │  localStorage│  │
│  │                  │  │                  │  │              │  │
│  │  - Users         │  │  - Rate Limits   │  │  - API Keys  │  │
│  │  - API Keys      │  │  - Sessions      │  │  (Dev only)  │  │
│  │  - Locations     │  │  - Cache         │  │              │  │
│  │  - Measurements  │  │                  │  │              │  │
│  │  - Usage Logs    │  │                  │  │              │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### 1. API Request Flow

```
User → Browser → API Request
                    ↓
            [Include X-API-Key header]
                    ↓
            Nginx/Load Balancer
                    ↓
            [SSL Termination]
                    ↓
            Next.js Middleware
                    ↓
        [CORS Check, Logging]
                    ↓
            API Route Handler
                    ↓
        [Validate API Key]
                    ↓
            ┌───────┴───────┐
            │               │
         Valid?          Invalid
            │               │
            ▼               ▼
    [Check Rate Limit]  [Return 401]
            │
            ▼
    [Query Database]
            │
            ▼
    [Format Response]
            │
            ▼
    [Log Request]
            │
            ▼
    Return JSON Response
```

### 2. API Key Creation Flow

```
User → /api-keys page
         ↓
    Click "Create New Key"
         ↓
    Enter key name
         ↓
    Click "Create Key"
         ↓
    Generate API Key
    (urx_[timestamp]_[random])
         ↓
    Hash API Key
    (SHA-256 + secret)
         ↓
    Save to Database/localStorage
    - key_hash
    - key_prefix (for display)
    - metadata
         ↓
    Show full key to user
    (ONLY ONCE!)
         ↓
    User copies key
         ↓
    Alert auto-hides after 30s
```

### 3. Authentication Flow

```
API Request with X-API-Key
         ↓
Extract API key from header
         ↓
Validate format
(regex: ^urx_[a-z0-9]+_[a-z0-9]+$)
         ↓
    ┌────┴────┐
Valid?      Invalid
    │           │
    ▼           ▼
Hash key    Return 401
    │
    ▼
Query database
(WHERE key_hash = ?)
    │
    ▼
┌───┴───┐
Found? Not Found
  │        │
  ▼        ▼
Check   Return 401
Active?
  │
  ▼
┌─┴─┐
Yes  No
 │    │
 ▼    ▼
Check Return 401
Rate
Limit
 │
 ▼
┌──┴──┐
OK   Exceeded
│      │
▼      ▼
Allow Return 429
Request
```

---

## 📦 Component Architecture

### Frontend Components

```
app/
├── api-keys/
│   └── page.tsx
│       ├── State Management
│       │   ├── apiKeys (array)
│       │   ├── visibleKeys (Set)
│       │   ├── copiedKey (string)
│       │   └── newlyCreatedKey (string)
│       │
│       ├── Functions
│       │   ├── generateAPIKey()
│       │   ├── createNewKey()
│       │   ├── deleteKey()
│       │   ├── toggleKeyVisibility()
│       │   ├── copyToClipboard()
│       │   └── maskKey()
│       │
│       └── UI Components
│           ├── Hero Section
│           ├── API Base URL Display
│           ├── Newly Created Alert
│           ├── Keys List
│           ├── Empty State
│           └── Create Modal
│
└── api-docs/
    └── page.tsx
        ├── Documentation Sections
        ├── Code Examples
        └── Copy Buttons
```

### Backend API Routes

```
app/api/v1/
├── locations/
│   └── route.ts
│       ├── validateAPIKey()
│       ├── GET handler
│       │   ├── Parse query params
│       │   ├── Filter data
│       │   ├── Paginate
│       │   └── Return response
│       │
│       └── POST handler
│           ├── Validate input
│           ├── Create location
│           └── Return response
│
├── measurements/
│   └── route.ts
│       ├── validateAPIKey()
│       ├── GET handler
│       │   ├── Parse query params
│       │   ├── Filter by parameter
│       │   ├── Filter by date range
│       │   └── Return response
│       │
│       └── POST handler
│           ├── Validate input
│           ├── Submit measurement
│           └── Return response
│
└── validate-key/
    └── route.ts
        └── POST handler
            ├── Check format
            └── Return validation result
```

---

## 🗄️ Database Schema

### Tables Relationship

```
┌─────────────────┐
│     users       │
│─────────────────│
│ id (PK)         │
│ email           │
│ name            │
│ password_hash   │
│ created_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│   api_keys      │
│─────────────────│
│ id (PK)         │
│ user_id (FK)    │◄───┐
│ name            │    │
│ key_hash        │    │
│ key_prefix      │    │
│ created_at      │    │
│ last_used_at    │    │
│ request_count   │    │
│ is_active       │    │
│ tier            │    │
└────────┬────────┘    │
         │             │
         │ 1:N         │
         │             │
         ▼             │
┌─────────────────┐    │
│ api_usage_logs  │    │
│─────────────────│    │
│ id (PK)         │    │
│ api_key_id (FK) │────┘
│ endpoint        │
│ method          │
│ status_code     │
│ response_time   │
│ ip_address      │
│ created_at      │
└─────────────────┘


┌─────────────────┐
│   locations     │
│─────────────────│
│ id (PK)         │
│ name            │
│ city            │
│ country         │
│ latitude        │
│ longitude       │
│ parameters      │
│ created_at      │
└────────┬────────┘
         │
         │ 1:N
         │
         ▼
┌─────────────────┐
│  measurements   │
│─────────────────│
│ id (PK)         │
│ location_id(FK) │
│ parameter       │
│ value           │
│ unit            │
│ timestamp       │
│ created_at      │
└─────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│         Layer 1: Transport              │
│  - HTTPS (TLS 1.3)                      │
│  - Certificate Validation               │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│         Layer 2: Network                │
│  - Firewall Rules                       │
│  - DDoS Protection                      │
│  - IP Whitelisting (optional)           │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│         Layer 3: Application            │
│  - CORS Configuration                   │
│  - Security Headers                     │
│  - Rate Limiting                        │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│         Layer 4: Authentication         │
│  - API Key Validation                   │
│  - Key Format Check                     │
│  - Key Hash Verification                │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│         Layer 5: Authorization          │
│  - Permission Check                     │
│  - Scope Validation                     │
│  - Tier Limits                          │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│         Layer 6: Input Validation       │
│  - Schema Validation (Zod)              │
│  - Sanitization                         │
│  - SQL Injection Prevention             │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│         Layer 7: Data                   │
│  - Encryption at Rest                   │
│  - Hashed Passwords                     │
│  - Audit Logging                        │
└─────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### Read Operation (GET)

```
Client Request
      ↓
[Validate API Key]
      ↓
[Check Rate Limit]
      ↓
[Parse Query Params]
      ↓
[Query Database]
      ↓
[Filter Results]
      ↓
[Paginate]
      ↓
[Format Response]
      ↓
[Update Usage Stats]
      ↓
[Log Request]
      ↓
Return Response
```

### Write Operation (POST)

```
Client Request
      ↓
[Validate API Key]
      ↓
[Check Permissions]
      ↓
[Validate Input Schema]
      ↓
[Sanitize Input]
      ↓
[Begin Transaction]
      ↓
[Insert to Database]
      ↓
[Commit Transaction]
      ↓
[Update Usage Stats]
      ↓
[Log Request]
      ↓
Return Response
```

---

## 🚀 Deployment Architecture

### Production Setup

```
┌─────────────────────────────────────────┐
│            CDN (Cloudflare)             │
│  - Static Assets                        │
│  - DDoS Protection                      │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│        Load Balancer (Nginx)            │
│  - SSL Termination                      │
│  - Request Distribution                 │
└────────────┬────────────────────────────┘
             │
      ┌──────┴──────┐
      │             │
      ▼             ▼
┌──────────┐  ┌──────────┐
│  App 1   │  │  App 2   │
│ (Next.js)│  │ (Next.js)│
└────┬─────┘  └────┬─────┘
     │             │
     └──────┬──────┘
            │
      ┌─────┴─────┐
      │           │
      ▼           ▼
┌──────────┐  ┌──────────┐
│PostgreSQL│  │  Redis   │
│(Primary) │  │  Cache   │
└──────────┘  └──────────┘
      │
      ▼
┌──────────┐
│PostgreSQL│
│(Replica) │
└──────────┘
```

---

## 📚 Related Documentation

- [API Endpoints](./API-Endpoints.md)
- [Security Best Practices](./Security-Best-Practices.md)
- [Deployment Guide](./Deployment-Guide.md)

